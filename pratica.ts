function multiplica(a: number, b: number): number {
    return a * b;
}

function sauda(name: string): string {
    return `Olá ${name}`;
}

const resultado = multiplica(3, 4);
const saudacao = sauda("João");